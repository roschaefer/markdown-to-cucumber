name: CI

on:
  push:
  pull_request:

jobs:
  ci:
    uses: roschaefer/reusable-workflows/.github/workflows/rust-ci.yml@1155bd6b30f7365cdfcfdbe628c9d014d49a2b26 # main
    with:
      binary_name: markdown-to-cucumber
